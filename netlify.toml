[build]
  base    = "artifacts/sozo-cannabis"
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/sozo-cannabis run build"
  publish = "dist/public"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
